[tools]
# ffmpeg = "C:/path/to/ffmpeg.exe"
# ffprobe = "C:/path/to/ffprobe.exe"

[whisper]
model = "small"
device = "cpu"
compute_type = "int8"
cpu_threads = 8
# language = "zh" # omit for automatic language detection
vad_filter = true
# cache_dir = "C:/path/to/model-cache"
