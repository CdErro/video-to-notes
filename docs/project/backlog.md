# 待办事项

## 高优先级

### 使用最终合并代码重跑三个小红书样例

- **当前情况**：短、中、长样例曾生成本地产物，但短/中样例早于 fail-closed 最终修复。
- **尚缺**：用 `tool-only@88bb916` 或后续等价提交完整重跑并保存 manifest、退出码和图片检查。
- **阻塞**：平台页面、登录态或网络可能变化。
- **范围**：`video_source`、Whisper、Provider、图片、渲染、一键流程。
- **验收**：成功样例使用真实单帧；Provider 失败样例显式失败，不生成伪笔记；可恢复执行。

### Bilibili 真实端到端

- **当前情况**：URL 检测、探测、下载代码和自动测试存在。
- **尚缺**：使用用户授权的 `--cookies-from-browser` 完成实际视频测试。
- **阻塞**：需要有效浏览器登录态和用户授权。
- **验收**：探测、下载、字幕/Whisper、笔记、图片和 manifest 全部完成，Cookie 不落盘。

### OpenAI 真实兼容服务验证

- **当前情况**：SDK 2.44.0 和 mock 测试通过。
- **尚缺**：用户通过 `.env` 提供 `OPENAI_BASE_URL`、`OPENAI_API_KEY` 后进行真实请求。
- **阻塞**：凭据和服务可用性。
- **验收**：字幕修正和最终多图笔记均返回符合 schema 的 JSON；日志和产物无 key。

### PDF 真实生成

- **当前情况**：Pandoc/XeLaTeX 调用和错误路径有测试，本机未安装工具。
- **尚缺**：安装依赖并生成包含中文、图片、公式/代码场景的真实 PDF。
- **验收**：`notes.tex`/`notes.pdf` 为 ready，中文字体和图片可读，`--resume --format pdf` 正常。

## 中优先级

### GitHub Actions CI

- **原因**：当前 PR 依赖本地测试，没有远端自动检查。
- **建议**：增加 Windows 与 Linux 的 Python 3.11+ 单元测试；外部工具和网络继续 mock。
- **验收**：PR 必需检查运行完整 suite，失败阻止合并。

### 同步上游镜像

- **当前情况**：2026-07-22 本地 `main` 相对 `origin/main` 落后 29 个提交；本地
  `tool-only` 与 `origin/tool-only` 一致。
- **建议**：按 `fetch upstream`、`merge --ff-only` 更新 main，再合入 tool-only；保持已删除发布目录。
- **验收**：`main` 仅含上游提交，`tool-only` 无丢失定制或归档回归。

### 扩展领域词典与 schema 版本策略

- **原因**：自动识别目前只有 `nju-os`；多个 JSON/TSV 产物已形成接口。
- **建议**：新增领域需提供种子、提示、误判测试；为 manifest、payload、audit 定义迁移策略。
- **验收**：旧运行目录仍可恢复或明确拒绝不兼容版本。

## 低优先级

### 性能与内容质量观测

- 记录视频时长、下载、Whisper、Provider、渲染分阶段耗时。
- 评估 CPU 长视频成本、50,000 字符截断和笔记质量等级是否合理。
- 在不放宽单帧证据约束的前提下评估镜头切换采样与相似帧去重。

### 发布与用户文档

- 增加常见错误排查、版本号、变更日志和示例输出。
- 明确支持平台是“由 yt-dlp 能力驱动”还是项目承诺的稳定接口。
- 发布前确认安装包/Skill 分发形式；现有记录无法确认该决策。
